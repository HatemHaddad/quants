from datetime import date

birthdate = date(1970,8,3) # The date class from the library datetime takes and integrt numbers as year, month and day seperated by commas.

# date attributes

birthdate.year
birthdate.month
birthdate.day
birthdate.weekday() #Monday == 0 ... Sunday == 6
